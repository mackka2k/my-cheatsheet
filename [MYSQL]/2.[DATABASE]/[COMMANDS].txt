CREATE DATABASE myDB;             [CREATES A DB NAMED "mydb"]
USE myDB;                         [USES SELECTED DATABASE]
DROP DATABASE myDB;               [REMOVES A DB NAMED "mydb"]
ALTER DATABASE myDB READ ONLY = 1 [ENABLES DB TO READ-ONLY]
ALTER DATABASE myDB READ ONLY = 1 [DISABLE DB TO READ-ONLY]