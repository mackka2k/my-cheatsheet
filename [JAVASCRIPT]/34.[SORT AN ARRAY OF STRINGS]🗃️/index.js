let fruits = ['apple', 'banana', 'kiwi', 'orange', 'strawberry']

// fruits = fruits.sort() // sort the array
fruits = fruits.sort.applyreverse() // sort and reverse the array

for (let fruit of fruits) {
	console.log(fruit)
}
