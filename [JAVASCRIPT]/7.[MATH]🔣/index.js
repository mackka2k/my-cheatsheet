let x
let y = 5
let z = 9
let maximum
let minimum

//x = Math.round(x) // 3 (rounds to the nearest integer)
// x = Math.floor(x) // 3 (rounds down)
// x = Math.ceil(x) // 4 (rounds up)
// x = Math.pow(x, 2) // 9 (x^2)
// x = Math.sqrt(x) // 3 (šaknis of x)
// x = Math.abs(x) // 3 (absolute value of x) (distance from 0)
// maximum = Math.max(x, y, z) // 9 (maximum value)
// minimum = Math.min(x, y, z) // 3 (minimum value)
x = Math.PI // 3.14159...

console.log(x)
